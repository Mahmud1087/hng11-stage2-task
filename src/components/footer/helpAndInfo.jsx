import { helpAndInfo } from './data';
import FooterContent from './footerContent';

const HelpAndInfo = () => {
  return <FooterContent title={'Help & Information'} data={helpAndInfo} />;
};
export default HelpAndInfo;
